# Gemini is activated only when both values are supplied by the deployment environment.
GEMINI_API_KEY=
GEMINI_MODEL=

# Optional Gemini REST configuration.
GEMINI_API_BASE_URL=https://generativelanguage.googleapis.com/v1beta
GEMINI_TIMEOUT_MS=60000
